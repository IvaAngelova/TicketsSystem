@page
@model TicketSystem.Pages.TicketPriorities.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<form asp-page="./Index" method="get">
    <div class="form-actions no-color">
        <p>
            Find by Priority name:
            <input type="text" name="SearchString" value="@Model.CurrentFilter" />
            <input type="submit" value="Search" class="btn btn-primary" />
            <a asp-page="./Index">Back to full List</a>
        </p>
    </div>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-page="./Index" asp-route-sortOrder="@Model.PriorityTypeSort"
                   asp-route-currentFilter="@Model.CurrentFilter">
                    @Html.DisplayNameFor(model => model.TicketPriority[0].PriorityType)
                </a>
            </th>
            <th>
                <a asp-page="./Index" asp-route-sortOrder="@Model.ModifDate17118162Sort"
                   asp-route-currentFilter="@Model.CurrentFilter">
                    @Html.DisplayNameFor(model => model.TicketPriority[0].ModifDate17118162)
                </a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.TicketPriority)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PriorityType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ModifDate17118162)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.TicketPriorityID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.TicketPriorityID">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.TicketPriorityID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
@{
    var prevDisabled = !Model.TicketPriority.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.TicketPriority.HasNextPage ? "disabled" : "";
}
<a asp-page="./Index"
   asp-route-sortOrder="@Model.CurrentSort"
   asp-route-pageIndex="@(Model.TicketPriority.PageIndex - 1)"
   asp-route-currentFilter="@Model.CurrentFilter"
   class="btn btn-default @prevDisabled">
    Previous
</a>
<a asp-page="./Index"
   asp-route-sortOrder="@Model.CurrentSort"
   asp-route-pageIndex="@(Model.TicketPriority.PageIndex + 1)"
   asp-route-currentFilter="@Model.CurrentFilter"
   class="btn btn-default @nextDisabled">
    Next
</a>